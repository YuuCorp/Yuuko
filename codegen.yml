overwrite: true
schema: 'https://graphql.anilist.co' # Replace with your GraphQL endpoint
documents: './src/graphQL/*.gql'
generates:
  ./src/graphQL/types/index.ts:
    plugins:
      - typescript
      - typescript-operations
