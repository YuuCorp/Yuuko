const Command = require("../Structures/Command.js");

module.exports = new Command ({
    name: "user",
    description: "Gets an anilist user and prints out the id and profile picture.",

    async run(message, args, run) {

    }
})
